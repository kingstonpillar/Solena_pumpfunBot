// solPriceFetcher.jsimport fetch from "node-fetch";// Jupiter API endpoint for fetching price quotesconst JUPITER_API_URL = "https://quote-api.jup.ag/v1/quote";// Function to fetch the SOL price using Jupiter APIexport async function fetchSolPriceUSD() {  try {    // Constructing the URL to fetch price quote from Jupiter's API    const url = `${JUPITER_API_URL}?inputMint=So11111111111111111111111111111111111111112&outputMint=USDm8kxyuR87Za6PLHR1wXvkmR8U4KYy8DLgZC8McLR2k`;  // SOL to USDC quote (example)    // Fetch the data from Jupiter API    const response = await fetch(url);    const data = await response.json();    // Check if data contains the required quote    if (data?.data?.price) {      const price = parseFloat(data.data.price);      console.log(`SOL Price in USD: ${price}`);      return price;    }    console.error("Failed to fetch SOL price from Jupiter API");    return null;  } catch (error) {    console.error("Error fetching SOL price:", error);    return null;  }}